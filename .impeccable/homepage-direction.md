# Homepage content update contract

Mode: Persuade. The user's latest correction is explicit: keep the previous aesthetic and change the context/content. The original code and CSS are visual authority. The discarded navy redesign and direction-seed experiment have no authority.

## First viewport

Retain AetherFlowHero, its full-height centered composition, purple particle canvas, gradient heading, pill, original type scale and entrance sequence. Replace its marketing message with the supplied real-estate enquiry-to-appointment copy, managed-system positioning and demo/process actions.

## Existing world

Use the unchanged tailwind.config.js and assets/css/input.css: near-black backgrounds, cyan primary #00f0ff, purple secondary #7000ff, Space Grotesk display and DM Sans body stacks, original button feedback, spotlight cards, gradient icon containers, connector tracks, stage badges and dark rounded CTA panel. No new global palette, custom typography declarations or page layout CSS.

## Content arrangement

Six requested sections, styled using existing component patterns. Lead process uses the original process cards; capabilities use the existing automation/spotlight card treatment; dashboard uses the actual project image in the original bordered/tilting product treatment; managed setup uses the original consulting stage badges and cards; final CTA uses the existing contact panel treatment. Original navigation and footer styling remain, with updated labels and destinations.

## Review boundaries

Preserve booking/chat API handlers, forms, consent, bilingual routes and reduced motion. Detailed pricing, comparison, FAQ, consulting and company content remains on separate pages as specified in the content brief. Browser surfaces are unavailable; report source and route checks without claiming screenshot review.
